name: test

on:
  push:
  pull_request:

jobs:
  shell-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Run test suite
        run: make test
